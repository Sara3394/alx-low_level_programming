#include "main.h"
/**
 * _abs - prints absolute value of an integer
 * @n: integer input
 *
 * Return: n which is the absolute value of the input
 */
int _abs(int n)

{
	if (n >= 0)
	{
		return (n);
	}
		return (-n);
}
